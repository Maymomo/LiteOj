const PREFIX = "http://127.0.0.1:8082";
const AUTHORIZE = PREFIX + "/authorize";
const LOGIN_URL = AUTHORIZE + "/login";
const REGISTER_URL = AUTHORIZE + "/register";

export {LOGIN_URL, REGISTER_URL};

